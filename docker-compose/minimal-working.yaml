services:
  alfresco:
    image: docker.io/alfresco/alfresco-content-repository-community:25.2.0
    ports:
      - "8080:8080"
    mem_limit: 3g
    environment:
      JAVA_TOOL_OPTIONS: >-
        -Dencryption.keystore.type=JCEKS
        -Dencryption.cipherAlgorithm=DESede/CBC/PKCS5Padding
        -Dencryption.keyAlgorithm=DESede
        -Dencryption.keystore.location=/usr/local/tomcat/shared/classes/alfresco/extension/keystore/keystore
        -Dmetadata-keystore.password=mp6yc0UD9e
        -Dmetadata-keystore.aliases=metadata
        -Dmetadata-keystore.metadata.password=oKIWzVdEdA
        -Dmetadata-keystore.metadata.algorithm=DESede
      JAVA_OPTS: >-
        -Ddb.driver=org.postgresql.Driver
        -Ddb.username=postgres
        -Ddb.password=devpassword123
        -Ddb.url=jdbc:postgresql://trust-server-postgres-local:5432/alfresco_prod
        -Dsolr.host=solr6
        -Dsolr.port=8983
        -Dsolr.http.connection.timeout=1000
        -Dsolr.secureComms=secret
        -Dsolr.sharedSecret=secret
        -Dsolr.base.url=/solr
        -Dindex.subsystem.name=solr6
        -Dshare.host=trustblocks.com
        -Dshare.port=80
        -Dalfresco.host=trustblocks.com
        -Dalfresco.port=80
        -Dcsrf.filter.enabled=false
        -Daos.baseUrlOverwrite=http://trustblocks.com/alfresco/aos
        -Dmessaging.broker.url="failover:(nio://activemq:61616)?timeout=3000&jms.useCompression=true"
        -Ddeployment.method=DOCKER_COMPOSE
        -DlocalTransform.core-aio.url=http://transform-core-aio:8090/
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/alfresco/api/-default-/public/alfresco/versions/1/probes/-ready-"]
      interval: 30s
      timeout: 3s
      retries: 5
      start_period: 1m
    volumes:
      - alf_data:/usr/local/tomcat/alf_data

  activemq:
    image: apache/activemq-classic:5.18.3
    mem_limit: 3g
    ports:
      - "61616:61616"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8161/admin/"]
      interval: 30s
      timeout: 3s
      retries: 5
      start_period: 10s

  solr6:
    image: docker.io/alfresco/alfresco-search-services:2.0.16
    mem_limit: 2g
    environment:
      SOLR_ALFRESCO_HOST: "alfresco"
      SOLR_ALFRESCO_PORT: "8080"
      SOLR_SOLR_HOST: "solr6"
      SOLR_SOLR_PORT: "8983"
      SOLR_CREATE_ALFRESCO_DEFAULTS: "alfresco,archive"
      ALFRESCO_SECURE_COMMS: "secret"
      JAVA_TOOL_OPTIONS: >-
        -Dalfresco.secureComms.secret=secret
    ports:
      - "8983:8983"

  transform-core-aio:
    image: alfresco/alfresco-transform-core-aio:5.2.0
    mem_limit: 1536m
    environment:
      ACTIVEMQ_URL: "nio://activemq:61616"
    ports:
      - "8090:8090"

  share:
    image: docker.io/alfresco/alfresco-share:25.2.0
    mem_limit: 1g
    environment:
      CSRF_FILTER_ORIGIN: http://trustblocks.com
      CSRF_FILTER_REFERER: http://trustblocks.com/share/.*
      REPO_HOST: "alfresco"
      REPO_PORT: "8080"
      JAVA_OPTS: >-
        -XX:MinRAMPercentage=50
        -XX:MaxRAMPercentage=85
        -Dalfresco.host=trustblocks.com
        -Dalfresco.port=80
        -Dalfresco.context=alfresco
        -Dalfresco.protocol=http
        -Dshare.context=share
        -Dapplication.title="TrustBlocks Document Management"
    ports:
        - "8081:8080"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/share"]
      interval: 20s
      timeout: 2s
      retries: 3
      start_period: 15s
    depends_on:
      alfresco:
        condition: service_healthy

volumes:
  alf_data:
    driver: local

networks:
  default:
    external: true
    name: trust-network